import 'package:flutter_test/flutter_test.dart';
import 'package:acrilc/services/websocket_service.dart'; // Adjust the import
import 'package:mockito/mockito.dart';
import 'helpers/mock_websocket.dart';

void main() {
  test('should send welcome message when connected', () {
    // final mockSocket = MockWebSocket();
    // final service = WebSocketService.test(mockSocket);

    // const welcomeMessage = 'Welcome to chat!';
    // service.send(welcomeMessage);

    // verify(mockSocket.add(welcomeMessage)).called(1);
  });
}
